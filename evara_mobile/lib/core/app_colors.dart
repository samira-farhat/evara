import 'package:flutter/material.dart';

class AppColors {
  // --- 1. Your Custom Palette (Light Theme Defaults) ---
  static const Color primary = Color(0xFFA78BFA);      // Soft Purple
  static const Color secondary = Color(0xFFC4B5FD);    // Light Lavender
  static const Color accent = Color(0xFFF8BBD9);       // Soft Pink Accent
  static const Color background = Color(0xFFF8F6FC);   // Off-White / Pale Lavender BG
  static const Color surface = Color(0xFFFFFFFF);      // Pure White (Cards/Containers)
  static const Color textPrimary = Color(0xFF2D2D2D);  // Dark Charcoal Text
  static const Color textSecondary = Color(0xFF6B7280);// Muted Gray Text

  // --- 2. Dark Twilight Galaxy Theme (Matches your Mockup Images) ---
  static const Color darkBackgroundTop = Color(0xFF2B1954);    // Rich violet top gradient
  static const Color darkBackgroundMiddle = Color(0xFF1B1135); // Twilight indigo middle
  static const Color darkBackgroundBottom = Color(0xFF10081E); // Deep fading bottom

  static const Color glassCardDark = Color(0x1AFFFFFF);        // Frosted glass overlay (White with low opacity)
  static const Color glassBorderDark = Color(0x4DFFFFFF);      // Subtle fine white border
  static const Color goldAccent = Color(0xFFD4AF37);           // Aged Champagne Gold Highlights
  static const Color eucalyptusSmoke = Color(0xFF8A9A90);      // Soft Muted Sage Secondary Text
}